apiVersion: v1
kind: Service
metadata:
  name: xnails-web
  namespace: xnails
  labels:
    app.kubernetes.io/name: xnails-web
spec:
  type: ClusterIP
  selector:
    app: xnails-web
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
