apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qualityspace-leads-data
  namespace: qualityspace
  labels:
    app.kubernetes.io/name: qualityspace-web
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
