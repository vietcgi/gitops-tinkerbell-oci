apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - release.yaml
  - grafana-oauth-sealed-secret.yaml
  - blackbox-exporter.yaml
  - loki.yaml
  - tempo.yaml
  - jaeger.yaml
  # ServiceMonitor and PrometheusRule resources moved to infrastructure-config layer (depend on kube-prometheus-stack CRDs)
  - promtail-daemonset.yaml
  - promtail-config.yaml
  - grafana-dashboards.yaml
