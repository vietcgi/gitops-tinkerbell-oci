apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # ServiceMonitor and PrometheusRule resources that depend on kube-prometheus-stack CRDs
  - network-monitoring.yaml
  - tinkerbell-alerting-rules.yaml
  - tinkerbell-service-monitors.yaml
  - bare-metal-health.yaml
  - tinkerbell-metrics.yaml
